# Copy to values.secrets.local.yaml on the server (gitignored), fill in real values.
#
# Used to populate the K8s Secret "yace-aws-creds" — see README.md for the kubectl command.
# This file is NOT passed to Helm; it's just a structured place to store the AWS keys
# so the README's `yq` lookups work consistently.

aws:
  accessKeyId: CHANGEME-AKIA...
  secretAccessKey: CHANGEME-aws-secret-access-key
