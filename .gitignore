# ─── SECRETS — never commit ───────────────────────────────────────
# secrets/<name>.yaml is the real (filled-in) version. Only *.yaml.example is committed.
secrets/*.yaml
!secrets/*.yaml.example

# Ansible inventory often holds host IPs / project metadata
agents/ansible/inventory.yml

# Generic
.env
*.local

# Editor / OS
.vscode/
.idea/
*.swp
*~
.DS_Store
Thumbs.db
